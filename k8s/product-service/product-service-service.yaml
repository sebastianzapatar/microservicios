apiVersion: v1
kind: Service
metadata:
  name: product-service
  labels:
    app: product-service
spec:
  ports:
    - port: 8084
      targetPort: 8084
  selector:
    app: product-service
  type: ClusterIP
