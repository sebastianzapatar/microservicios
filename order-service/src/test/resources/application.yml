spring:
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5433/orders_test
    username: postgres
    password: ""
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
eureka:
  client:
    enabled: false
